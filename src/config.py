# This is the Chromabot config file, it should be located outside of
# the project folder. Examples being in `$HOME/.config/chromabot/`, or
# `/etc/chromabot/` to not accidentally commit tokens.

config = {
    'AskForToken' : True,
    'DadBotEnabled' : False,
    'Token' : ""
    }
